// test if statements are correctly generated and executed

static if (0 == 0) {
	raise(1, raise.debug, "0 == 0", null);
}

static if (0 != 0) {
	raise(2, raise.debug, "0 != 0", null);
}

static if (0 == 0) {
	raise(3, raise.debug, "0 == 0", null);
}
else {
	raise(4, raise.debug, "0 != 0", null);
}

static if (0 != 0) {
	raise(5, raise.debug, "0 != 0", null);
}
else {
	raise(6, raise.debug, "0 == 0", null);
}

{
	int t = 0;

	if (t == 0) {
		raise(7, raise.debug, "t == 0", t);
	}

	if (t != 0) {
		raise(8, raise.debug, "t != 0", t);
	}

	if (t == 0) {
		raise(9, raise.debug, "t == 0", t);
	}
	else {
		raise(10, raise.debug, "t != 0", t);
	}

	if (t != 0) {
		raise(11, raise.debug, "t != 0", t);
	}
	else {
		raise(12, raise.debug, "t == 0", t);
	}
}

static if (typename(integer) == null) {
error("integer is not declared");
}

static if (typename(integer) != typename) {
error("integer is not a type");
}
