enum Window {
	draw = RenderMode.swap_buff;
	//~ resx = properties.screen.width;
	//~ resy = properties.screen.height;
}

/* list arguments
debug(arguments);
// */

//~ string filename = "media/images/Earth.jpg";
setupPicView(gxSurf(filename), true);
