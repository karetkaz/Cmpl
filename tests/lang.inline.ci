
inline min(int a, int b) = a < b ? a : b;

inline max(int a, int b) = a > b ? a : b;

inline mxa(int a, int b) = a ? a : b;

inline mx0(int a, int b) = 0;

int a = 3;

int b = 6;

int m0 = min(a, b);

int m1 = max(a, b);

int m2 = mxa(0, b);

int m3 = mx0(a, b);