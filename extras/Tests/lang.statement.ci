// test if statements are correcly generated

static if (0 == 0) {
	raise(1, "0 == 0", null, 1);
}

static if (0 != 0) {
	raise(1, "0 != 0", null, 2);
}

static if (0 == 0) {
	raise(1, "0 == 0", null, 3);
}
else {
	raise(1, "0 != 0", null, 4);
}

static if (0 != 0) {
	raise(1, "0 != 0", null, 5);
}
else {
	raise(1, "0 == 0", null, 6);
}

{
	int t = 0;

	if (t == 0) {
		raise(1, "t == 0", variant(&t), 7);
	}

	if (t != 0) {
		raise(1, "t != 0", variant(&t), 8);
	}

	if (t != 0) {
		raise(1, "t != 0", variant(&t), 9);
	}
	else {
		raise(1, "t == 0", variant(&t), 10);
	}

	if (t != 0) {
		raise(1, "t != 0", variant(&t), 11);
	}
	else {
		raise(1, "t == 0", variant(&t), 12);
	}
}
