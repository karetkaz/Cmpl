// test for statements are correctly generated

for ( ; ; ) {
	raise(debug, "for ( ; ; )", null, defTrace);
	break;
}

int forIdx;
for (forIdx = 0; forIdx < 2; forIdx += 1) {
	raise(debug, "forIdx", variant(forIdx), defTrace);
}

for (int i = 0; i < 2; i += 1) {
	raise(debug, "for.i", variant(i), defTrace);
}

/* TODO: Unimplemented feature
int array[20];
for (int i: array) {
	raise(debug, "for", variant(i), 7);
}
// */
