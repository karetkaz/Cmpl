// TEST: overload

inline overload = 1;
inline overload() = 2;
inline overload(int a) = 3;
inline overload(float a) = 4;
inline overload(int a, int b) = 5;

float32 overload1 = overload;
float32 overload2 = overload();
float32 overload3 = overload(0);
float32 overload4 = overload(0.);  // FIXME: uses `inline overload = 1;`
float32 overload5 = overload(0, 0);
