
struct DebugInfo {
	string  function;
	float64 expected;
	float64 returned;
}

DebugInfo info = DebugInfo("fun1", 3.14, 3.15);
void almaf(int x) {
	int a23;
	int a[] = emit(struct, int(0), int(0));
	//~ debug("Fatal error", variant(&info), -1, 0);
	//~ int8 fasterstackOverflow[8 << 20 - 1];
	a[0] = x;
	almaf(x + 1);
}
//~ debug("Fatal error", variant(&info), -1, 0);
almaf(0);
abort();
assert(!true);
System.exit(0);
