float a[100][100], b[100][100], c[100][100];
/*							factorial
{
	int i, n; n = 10;
	static int e1, e2, e3;
	e1 = e2 = e3 = 1;
	{
		fork {
			for (i = 1; i < n/3; i += 1) {
				e1 *= i;
			}
		}
		fork {
			for (i = n/3; i < n/2; i += 1) {
				e2 *= i;
			}
		}
		fork {
			for (i = n/2; i <= n; i += 1) {
				e3 *= i;
			}
		}
	}
	print(e1*e2*e3, "\n");		//3628800
}//*/
//~ /*							Matrix multiply
{
	int i, j, k, n; n = 10;
	for(i = 0; i < n; i += 1) {
		for(j=0;j<n;j+=1) {
			b[i][j] = c[i][j] = (i==j)+1;
		}
	}
	for(i = 0; i < n; i += 1) fork {
		for(j = 0;j < n;j += 1) {
			a[i][j] = 0;
			for(k = 0; k < n; k += 1) {
				a[i][j] += b[i][k] * c[k][j];
			}
		}
	}
	for(i = 0; i < n; i += 1) {
		for(j = 0; j < n; j += 1) {
			print(a[i][j], " ");
		}
		print("\n");
	}
}//*/
