//~ module test_what_does_not_work;

//~ int alma(int h)[10];

/* cgen: int64(8+12): abs(n)
define abs(int64 ^x) = (x < 0 ? -x : x);

int n = -9;
int z = abs(n);
// */

//~ pointer x = emit(pointer, "er");
//~ string str = emit(string, ref(x));
struct ss {
	int x;
	int y;
}

void test1() {
	void test2() {
		static ss v = ss(1,2);
		v.x = 8;
	}
}
