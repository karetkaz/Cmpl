import ../../SciTEDirectory
import ../SciTEDirectory

api.$(ccvm.fext)=../extras/stdlib.api

if PLAT_GTK
	ccvm.exec=cd .. && ./main
if PLAT_WIN
	ccvm.exec=cd .. && .\main

command.go.$(ccvm.fext)=$(ccvm.exec) -debug "$(FilePath)"
