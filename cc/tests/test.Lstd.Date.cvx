// test date functions
char format_date_buff[1024] = [0];
string format_date_str = string(format_date_buff);

//~ Date now = DateFromTime(time());
//~ Date.Parts parts = Date.ToParts(now.days);

//~ format(format_date_buff, "%Y.%m.%d %H:%M:%S", DateFromTime(time()));
format(format_date_buff, "%A %d %B %Y", DateFromTime(System.time()));

int eq = compare("..a", "..b");
