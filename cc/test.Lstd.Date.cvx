// test date functions
char buff[1024] = [0];
string str = string(buff);

//~ Date now = DateFromTime(time());
//~ Date.Parts parts = Date.ToParts(now.days);

//~ format(buff, "%Y.%m.%d %H:%M:%S", DateFromTime(time()));
format(buff, "%A %d %B %Y", DateFromTime(time()));

