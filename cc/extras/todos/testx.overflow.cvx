// test
struct DebugInfo {
	string  function;
	float64 expected;
	float64 returned;
}

DebugInfo info = DebugInfo("fun1", 3.14, 3.15);
void stackOverflow(int x) {
	int a23;
	int a[] = emit(struct, pointer(null), int(0));
	//~ debug("Fatal error", variant(&info), -1, 0);
	//~ int8 fasterStackOverflow[8 << 20 - 1];
	//~ a[0] = x;
	stackOverflow(x + 1);
}
//~ debug("Fatal error", variant(&info), -1, 0);
stackOverflow(0);
abort();
assert(!true);
System.exit(0);
