define wrap = 80;
define chars = 95;
char result[1024];
//~ string text = string(result);

for (int i = 0; i < result.length - 1; i += 1) {
	result[i] = ' ' + (i - i / wrap) % chars;
	if (i > 0 && i % wrap == 0) {
		result[i - 1] = '\n';
	}
}
//~ editor.setText(result);
